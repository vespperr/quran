workflows:
  ios-release:
    name: iOS Release Build (iOS 15.1 Target)
    max_build_duration: 60
    instance_type: mac_mini_m1
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
    scripts:
      - name: Set up Flutter dependencies
        script: |
          flutter packages pub get
      - name: Build iOS IPA (Targeting iOS 15.1)
        script: |
          IPHONEOS_DEPLOYMENT_TARGET=15.1 flutter build ios --release --no-codesign
          
          # Create IPA payload
          cd build/ios/iphoneos
          mkdir -p Payload
          cp -r Runner.app Payload/
          zip -r Raiyan.ipa Payload
          
          # Copy to root artifacts folder
          mkdir -p $CM_BUILD_DIR/build/ios/ipa
          cp Raiyan.ipa $CM_BUILD_DIR/build/ios/ipa/
    artifacts:
      - build/ios/ipa/*.ipa
      - build/ios/iphoneos/*.app
